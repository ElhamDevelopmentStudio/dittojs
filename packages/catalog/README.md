# @dittosh/catalog

The versioned manifest catalog and generated dependency policy for DittoJs.

Catalog modules are resolved by `@dittosh/core`; dependency compatibility must not be implemented
in consuming UI code.
