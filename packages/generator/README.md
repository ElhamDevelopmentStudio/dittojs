# @dittosh/generator

The filesystem-safe project generator for resolved DittoJs recipes.

The generator accepts resolver output and writes a standalone project. It does not resolve module
requirements itself.
